spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

server:
  port: 8082

# Node.js : SmartThings Service
smartthings:
  service:
    url: https://k12d103.p.ssafy.io/smart
